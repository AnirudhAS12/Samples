class Animal
{
    constructor(name,sound)
    {
        this.name=name;
        this.sound-sound;
    

    this.getSound = ()=>{
        return `${this.name} creates a sound ${this.sound}`;
    }
    }
    
}